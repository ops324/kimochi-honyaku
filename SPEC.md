# きもち翻訳 — プロジェクト仕様書

最終更新: 2026年5月10日（入力画面UX改善・全画面キラキラ装飾レイヤー・翻訳中/結果画面の世界観統一・フィードバック言いなおし導線）

---

## プロジェクト概要

**アプリ名**: きもち翻訳  
**コンセプト**: 「うまく言えない体や気持ちの状態」を、断片的なタップ入力からAIが翻訳し、親や医師、学校の先生に伝わる形に変換するアプリ。不調時だけでなく、調子がいい日や言葉にならない日の記録にも使える。

### 解決したい課題
- 子どもが「なんとなくしんどい」としか言えないとき、細かく説明させると本人の感覚から遠ざかる
- 正確な記録よりも「伝わること」がゴール
- 言語化・構造化しすぎると「説明される」になってしまう
- 単一の症状ではなく**組み合わせ**で意味を持つ不調（起立性調節障害など）を、子どもに難しい言葉を使わずに伝える
- **「しんどい / 元気」の二項対立**に当てはまらない日（なんとなく変、言葉にならない）も記録できること
- **症状の選択肢を見ること自体が辛い日**（極度のしんどさ）でも、1タップで記録を完了できること

### 設計の核心
- **翻訳の粒度**: AIがどこまで補完し、どこを曖昧なまま残すかのバランス
- **使われ続ける理由を丁寧に設計する**: 記録させるではなく、使いたくなる状態を作る
- **子ども側と大人側で語彙を分ける**: 子どもには優しい言葉、医師や学校には所見・配慮事項を伝える
- **記録しない自由を保障する**: 「今日は使わない」を選べることが継続の前提
- **底の日の脱出口を用意する**: 選択肢を見ることすら辛い日に「選べないくらいしんどい…」1タップで記録を完了できる。何も選べなくても「今日は記録した」という事実を残せる。

---

## ファイル構成

```
見えない不調を翻訳する/
├── index.html       # アプリ本体（ビルド不要、ブラウザで直接開く）
├── style.css        # スタイル（デザイントークンベース）
├── app.js           # ロジック（現在はモックAI）
├── SPEC.md          # この仕様書
└── .claude/
    └── launch.json  # プレビューサーバー設定（port 3002）
```

---

## 画面構成（5画面）

### 画面0: ウェルカム（`#screen-welcome`）
- **時刻に応じたあいさつメッセージ**（5段階: 朝/昼/夕/夜/深夜、各2〜3バリアント）
  - 同じ時間帯でも日替わりでメッセージが変わる（`dayIndex % variants.length`）
- タイトル「きもち翻訳」（単色ダークネイビー、letter-spacing 0.18em、テキストシャドウで光に浮かせる）
  - ~~グラデーションタイトル~~ → 2026-05-09 に単色化
  - ~~カスタムSVGゴーストキャラクター（浮遊アニメーション）~~ → 2026-05-09 に削除
- **ストリークバッジ**（2回以上の記録で表示。localStorage管理）
  - 文言: 傾聴フレーム「あなたの声を ${n}回 きいてきたよ」（n≤6）
  - 長期継続バリアント（7回以上）: 回数ではなく在り続けた感を強調
  - 「X日連続達成」「記録継続中」などの義務感ある表現は使わない
  - スタイル: `backdrop-filter: blur(8px)` のガラス調ピル（背景ミストに溶け込む）
- サブテキスト「うまく言えなくても大丈夫」（**CTAの直上** に配置）
  - ~~CTAの下~~ → 2026-05-09 にCTA上へ移設
- **CTAボタン**「今日のじぶんを ことばにする」（`.cta-btn--welcome` クラス）
  - 白ピル + ソフトシャドウ + ダーク文字（`rgba(255,255,255,0.92)`）
  - shimmer アニメーションなし（入力画面CTAのグラデ仕様とは別クラスで管理）
- **脱出口ボタン**（ゴーストスタイル）「今日は記録しない」
  - タップ時: ボタンが消え、その場に「記録しない日も、ある。また来てね。」と3.2秒表示（`.soft-confirm`）
  - 責めず、次回を誘う一言で終わる

### 画面1: 入力（`#screen-input`）
- トップバー: 戻るボタン + 日付
- タイトル「今、どんな感じ？」
- **3択モード切り替えトグル**（`role="tablist"`、各ボタン`role="tab"`）
  - **しんどい感じ**（`#toggle-unwell`、デフォルト）
  - **うまく言えない**（`#toggle-unsure`）
  - **いい感じ**（`#toggle-good`）
  - 切り替え時: 選択状態リセット、CTAの活性条件を更新

- **クイック記録ボタン**「選べないくらいしんどい…」（2026-05-10 〜）
  - モードトグルとカードグリッドの間に配置（`#quick-record-wrap`）
  - **しんどいモード限定**（`#toggle-unwell` 選択時のみ表示。モード切替で自動表示/非表示）
  - タップすると即座にローディング→翻訳結果画面へ遷移（カード選択・メモ不要）
  - 空タグで翻訳した場合の専用文面:「今日はとても体調がつらく、症状を言葉にするのも難しいほどしんどい状態にある。」
  - デザイン: 白ピル + hairline + backdrop-blur。ウェルカム画面の「今日は記録しない」と同じ"逃げ道"トーン。CTA と並置して「選べなくていい」を視覚的に伝える
  - ストリーク・`kmt_records` への保存は通常フローと同様に行われる

- **カードグリッド選択**（3列グリッド、複数選択可）

  **段階的開示（2026-05-10 〜）**
  - 初期表示: 各セクション最初の3枚（= 3列グリッドの1行分）のみ表示
  - 4枚目以降: CSS `nth-child(n+4)` で初期非表示（`#set-unwell` / `#set-good` のみ対象）
  - 「もっと選ぶ（+N）」ボタン（`.expand-btn`）をカードグリッド直後に自動生成（`initCardExpanders()`）
    - 隠れるカードが1枚以下（4枚セクション）の場合はボタンを出さず全表示
    - クリックするとグリッドに `.expanded` クラスを付与して全展開、ボタン消滅（折りたたみなし）
  - `#set-unsure`（4枚）は対象外 — 全表示
  - 狙い: しんどい時に「全部見なくていい」を視覚的に保証。気力があれば展開できる

  **しんどい感じモード（`#set-unwell`）**
  - **体のこと** [`data-category="body"`]: 頭が重い / だるい / めまいがする / 吐き気がある / 体が痛い / 食べられない
  - **ねむり** [`data-category="sleep"`]: 眠れなかった / 寝つけなかった / 夜中に起きた / 寝ても疲れがとれない / 怖い夢を見た
  - **気持ちのこと** [`data-category="mood"`]: 不安な感じ / 落ち込んでいる / ぼーっとする / 何もしたくない / イライラする / 泣きたい感じ
  - **状況のこと** [`data-category="situation"`]: 朝起きられない / 学校に行けない / 集中できない / 外に出たくない / 人と話したくない

  **うまく言えないモード（`#set-unsure`）**
  - [`data-category="unsure"`] の汎用カード4枚:
    - なんとなくしんどい / 言葉にならない / いつもと違う / もやもやする
  - **メモ欄のみでも翻訳可能**（カード0枚 + メモあり → CTAが活性化）
  - セクション末尾の補助テキスト（`.unsure-note`）:
    「カードを選ばなくてもいいよ。ことばにならない感じ、そのまま教えてね。」

  **いい感じモード（`#set-good`）**
  - **体のこと**: 体が軽い / 食欲がある / 頭がすっきり / 体に力がある / 気分がいい
  - **ねむり**: よく眠れた / すぐ眠れた / すっきり起きた / いい夢を見た
  - **気持ちのこと**: 楽しい気分 / 落ち着いている / やる気がある / 嬉しい感じ / ほっとしている / 安心している
  - **状況のこと**: 朝起きられた / 学校に行けた / 集中できた / 外に出られた / 人と話せた

- **客観値の任意手入力**（しんどい/いい感じ両モードで「ねむり」セクション末尾に表示）
  - **保護者向け補助フィールド**（`<details id="parent-panel">`、デフォルト閉じた状態）
  - 開閉テキスト: 「保護者の方へ — スマートウォッチなどで分かれば（任意）」
  - 前回入力した値がある場合はプレースホルダーに参考値を表示し、バッジ「前回の記録があります」を表示（`initParentPanelMemory()`）
  - ねむった時間: 0.5刻みの number input（h単位、空欄可）
  - 眠りの深さ: 5段階セグメントボタン（1=浅い〜5=深い）
  - 入力された値は医師・学校向け翻訳に同梱される
  - **子どものメインフローの最後（フォーム末尾）** に配置 → 子どもの選択体験を妨げない

- ひとことメモ（テキストエリア、任意）
  - うまく言えないモードではメモ入力でCTAが活性化する
- **CTA**「ことばに してもらう ✦」（1つ以上選択、またはうまく言えないモード時はメモ入力で活性化）
  - 非活性時のヒント: 「1つでも見つかったら、ことばにしますね。」
  - 活性化は `syncCTA()` が管理（モードとselected/memoを考慮）
  - スタイル: 単色 `--brand` (#7C6FE0) + inset ハイライト + ソフトドロップシャドウ。shimmer アニメーションなし（2026-05-09 〜）

### 画面2: 翻訳中（`#screen-loading`）
- **「空間と光が主役」の構成**（2026-05-10 〜）
  - 中央: 光のオーブ `.loading-orb-core`（白→ラベンダーの radial-gradient + box-shadow による多層ハロー、4.2s の呼吸）
  - 周辺: 拡散リング2層 `.loading-orb-ring`（時間差 1.8s で外側へ広がりながらフェードアウト、3.6s ループ）
  - 背景: `#ambient-decor` のキラキラ層がそのまま透けて連続する（screen-loading 自体は背景透明）
  - ~~ゴーストキャラクター（バウンス）+ 3色ドット~~ → 2026-05-10 に廃止
- 「あなたのきもちを ことばに しています…」
- 所要時間: 2.2秒（APIコール想定）
- `prefers-reduced-motion: reduce` の場合: オーブとリングのアニメーションを停止し、リングは `opacity: 0.35` 固定で静止表示

### 画面3: 翻訳結果（`#screen-result`）
- **ムードカード**（ヒーロー要素・共有可能）
  - 色彩心理学に基づく配色（しんどい=ネイビーブルー / うまく言えない=モーブパープル / いい感じ=ゴールデンイエロー）
  - 翻訳テキスト（基本翻訳のみ。組み合わせ所見や時系列サマリは出さない）
  - タグ一覧（選択したカード）
  - **意図的に他のUIと色のコントラストを残す**（感情の重みを担うヒーロー要素のため、glass化しない）
- **フィードバックブロック**（`#feedback-block`、glass化 2026-05-10 〜）
  - 半透明白 + backdrop-blur + ラベンダーミストの hairline。`#ambient-decor` の光が透ける
  - 初期状態 `opacity: 0`、翻訳完了から **1.8秒後** に `opacity: 1`（`.appeared` クラス付与、540ms transition）
  - 狙い: 翻訳文を先に読む時間を確保し、判断の急かしを避ける（感情への共鳴余地を設ける）
  - 3択絵文字: ちがう / 近い / そのとおり
- **言いなおし導線**（`#feedback-refine`、2026-05-10 〜）
  - 「ちがう」「近い」を選んだときだけ展開（`.visible` で fade + slide）。「そのとおり」では出さない
  - 構成: 中立コピーのラベル「もう少し おしえてくれる？（書かなくても大丈夫）」 + 自由記述 textarea（任意） + 「言いなおしてみる」ピル（lavender）
  - placeholder: 「ちがった感じ、ちかい感じ、ひとことだけでも…」
  - **書かなくてもよい**: 単に「言いなおしてみる」を押せば入力画面に戻れる。書く気力がない時の沈黙を尊重
  - 「言いなおしてみる」を押すと:
    - textarea に内容があれば最後の record に `refineNote` として追記
    - `state.selected` / `state.memo` は保持されたまま `screen-input` に遷移（カードの `.selected` 状態も復元）
    - 同日の再翻訳でも `bumpStreak()` は冪等（`readLastDay() === today` で早期 return）なので二重カウントにならない
  - record への永続化フィールド: `feedback: 'wrong' | 'close' | 'exact'`、`refineNote?: string`
- **共有アクション**（4ボタン、2×2グリッド、glass化 2026-05-10 〜）
  - 半透明白 + backdrop-blur + ラベンダー hairline
  - アイコンは入力カードと同じラベンダーミスト世界観の自作SVG（インライン）
    - 親に見せる: 大人/子のシルエット（薄ラベンダー塗り＋紫ストローク）
    - 医師に渡す: 救急バッグ + ピンクの十字
    - 学校に渡す: スパイラルノート + ライン + ゴールド星
    - 画像で保存: 額縁 + ラベンダーの山並み + ゴールドの太陽
- 「きょうは ここまで」ゴーストボタン（glass化、結果画面を閉じてウェルカムへ戻る）

### 画面4: 共有プレビュー（`#screen-share`）
- モード: **親向け / 医師向け / 学校向け** で文面・アイコン・含まれる情報が変わる
- ドキュメント型カード + 「テキストをコピー」ボタン
- コピー時のヘッダー:
  - 親向け: 「【親への共有メモ】」
  - 医師向け: 「【受診時の参考メモ】」
  - 学校向け: 「【学校への共有メモ】」

---

## AIロジック（現在: モック）

### 翻訳パイプの2段階

```
[選択タグ + メモ + 客観睡眠値 + モード]
   ↓
1. translate(tags, memo, mode)  — 子ども・親に見せる本文
   ↓
2. formatForAudience(audience, ctx) — 提出先別に最終文面を生成
   ↓
[親向け / 医師向け / 学校向け]
```

### 1. translate (基本翻訳)

選択されたタグをカテゴリ別・モード別に自然な日本語文に変換する。

**しんどい感じモード** (`mode === 'unwell'`):
```
[体] + [ねむり] + [気持ち] + [状況] + [メモ] → 1〜4文の要約
```
接続詞: 体「。」→ ねむり「また、」→ 気持ち「また、」→ 状況「そのため、」→ メモ「本人からは「〜」とのこと。」

**クイック記録（タグ0件 + unwell モード）**（2026-05-10 〜）:
- 「今日はとても体調がつらく、症状を言葉にするのも難しいほどしんどい状態にある。」
- メモがある場合: 末尾に「本人からは「〜」とのこと。」を付加
- 既存フォールバック（`'はっきりとした原因はわからないが…'`）を置き換える

**うまく言えないモード** (`mode === 'unsure'`):
- UNSURE_MAP（4枚のカード用の短文フレーズ）から断片を拾い、「なんとなくいつもと違う感じがある」を基底として繋ぐ
- カード0枚 + メモのみの場合:「今日は言葉にならない何かがある。本人いわく「〜」とのこと。」
- いずれも「言語化を強いない」文体を保つ

**いい感じモード** (`mode === 'good'`):
- 接続詞を「さらに、〜」中心にして肯定的に。

### 2. 組み合わせシグナル検出 (detectSignals)

選択タグの組み合わせを **ルールベース** で判定し、医師・学校向け翻訳に「考えられるパターン」として注記する。**親向け・子ども向けには出さない**。

| ID | 名称 | マッチ条件 |
|---|---|---|
| `od` | 起立性調節障害（OD）の典型像 | `朝起きられない` ∧ `めまいがする` ∧ (`頭が重い` ∨ `だるい`) |
| `headache` | 頭痛発作の示唆 | `頭が重い` ∧ (`吐き気がある` ∨ `めまいがする`) |
| `circadian` | 概日リズム睡眠の乱れ | `朝起きられない` ∧ (`寝つけなかった` ∨ `夜中に起きた` ∨ `眠れなかった`) |
| `anxiety` | 不安・緊張反応の示唆 | `不安な感じ` ∧（`寝つけなかった`・`食べられない`・`泣きたい感じ` のうち2つ以上） |
| `low_energy` | エネルギー低下の遷延 | `だるい` ∧ `落ち込んでいる` ∧ (`何もしたくない` ∨ `食べられない`) |

優先度は **OD > headache > circadian > anxiety > low_energy**、最大2件まで注記する。確定診断は匂わせず「典型像と類似する組み合わせ」と表現する。

うまく言えないモードではシグナル検出をスキップする（カード4枚の意味が具体的症状でないため）。

### 3. 提出先別フォーマッタ (formatForAudience)

| audience | アイコン | トーン | 含める要素 |
|---|---|---|---|
| `parent` | 👨‍👩‍👧 | 子ども目線・優しい | 翻訳本文 + タグ（睡眠は「短め/浅め」等のソフト表現） |
| `doctor` | 🏥 | 医学的SOAP風 | **S**: 本人の訴え / **O**: 選択タグ・睡眠客観値 / **A**: 組み合わせ所見 / **P**: 継続観察と最近30日の傾向サマリ |
| `school` | 🏫 | 配慮事項中心・病名暗示控えめ | 教室での配慮事項 + 直近の登校状況 + 【背景にある発達特性】節 |

**うまく言えないモードでの各audience処理**:
- `parent`: 「今日は言葉にしにくい状態でした」として翻訳本文を添える
- `doctor`: SOAP【A】に「非特異的主観的愁訴（non-specific subjective complaint）」と記載。特定シグナルへの帰属は行わない
- `school`: 「体調が優れず、言語化しにくい状態です」「無理に詳しく話させず、選択肢を提示してください」を推奨事項として記載

**学校向けの【背景にある発達特性】節**（思春期・概日リズム関連シグナルがある場合のみ）:
- 病名は出さず「思春期の体内時計の変化」として説明
- 根拠: 志村哲祥『寝た子は起こすな』NHK出版, 2026（思春期の睡眠相後退は生物学的特性）

### 4. 共有テキスト生成

```
【親への共有メモ / 受診時の参考メモ / 学校への共有メモ】
YYYY年MM月DD日（曜）

{formatForAudience の結果}

記録されたこと：{タグ一覧}

きもち翻訳より
```

---

## 状態管理（app.js）

```js
const state = {
  selected:     Set,                       // 選択中のカード（JSON文字列）
  memo:         string,                    // メモテキスト
  translation:  string,                    // 基本翻訳結果
  tags:         Array,                     // { val, cat } の配列
  shareMode:    string,                    // 'parent' | 'doctor' | 'school'
  feedbackDone: boolean,                   // フィードバック済みフラグ
  mode:         string,                    // 'unwell' | 'unsure' | 'good'
  sleepHours:   number | null,             // 客観睡眠時間（任意）
  sleepScore:   1 | 2 | 3 | 4 | 5 | null, // 眠りの深さ（任意）
  signals:      string[],                  // detectSignals の結果（id配列）
  quickRecord:  boolean,                   // クイック記録経由フラグ（2026-05-10 〜）
}
```

### ストリーク管理（localStorage）
- `kmt_streak`: 累計記録回数（数値）
- `kmt_last`: 最終記録日（YYYY-M-D 文字列）
- 翻訳実行時に `bumpStreak()` を呼び出し
- 2回以上でウェルカム画面にバッジ表示
- 文言: 傾聴フレーム（「声を聞いてきた」視点）— 義務感・プレッシャーを与えない

### 記録履歴の永続化（localStorage）
- `kmt_records`: 過去の記録の配列（直近180日まで保持）

```js
// kmt_records の各要素
{
  id: number,                         // Date.now()
  date: 'YYYY-MM-DD',
  mode: 'unwell' | 'unsure' | 'good',
  tags: [{ val, cat }],
  memo: string,
  sleepHours: number | null,
  sleepScore: number | null,
  signals: string[],                  // unsure モード時は []
  translation: string,
  // 以下、フィードバック後に updateLastRecordFeedback() が追記する任意フィールド
  feedback?: 'wrong' | 'close' | 'exact',
  refineNote?: string,                // 「言いなおしてみる」前の自由記述（任意・トリム済み）
}
```

### 傾向サマリ (summarizeRecent)

直近N日の記録を集計して医師・学校向け翻訳に同梱する。

```js
summarizeRecent(days = 30) → {
  totalDays,            // 期間中の記録日数
  unwellDays,           // しんどいモードだった日数
  topTags,              // 頻出タグ Top3（unsureタグは除く）
  morningStruggleCount, // '朝起きられない' 出現日数
  sleepHoursMedian,     // 睡眠時間の中央値（客観値）
  recurringSignals,     // 同じ signal の繰返し出現
}
```

### 保護者パネルの前回値記憶 (initParentPanelMemory)

`kmt_records` を後ろから走査し、`sleepHours` または `sleepScore` がある最新レコードを探す。
見つかった場合:
- `sleepHours` input の placeholder に「前回: X.Xh」を表示
- `sleepScore` セグメントに前回値をデフォルト選択
- パネル summary 要素に `.parent-panel-memo`（バッジ）を追加

---

## デザインシステム

### カラーパレット（デザイントークン）

| トークン | 値 | 用途 |
|---|---|---|
| `--bg` | `#EAE5F2` | ページ背景（ラベンダーミスト） |
| `--bg-deep` | `#DCD4EA` | 周縁グラデの落とし所 |
| `--surface` | `#FFFFFF` | カード背景 |
| `--surface-glass` | `rgba(255,255,255,0.85)` | ガラス調要素（ウェルカムCTAなど） |
| `--border` | `#E2DCEE` | 境界線（CSS変数。実際は `rgba(30,27,46,0.08)` のhairlineで上書き） |
| `--ink-1` | `#1E1B2E` | 本文テキスト |
| `--ink-2` | `#6B6478` | サブテキスト |
| `--ink-3` | `#ADA6B8` | ヒント・プレースホルダー |
| `--brand` | `#7C6FE0` | ブランドカラー（入力CTA・選択カード枠・チェックバッジに使用） |
| `--grad-brand` | `#7C6FE0 → #B39DFA → #F9A8C9` | グラデーション（共有画面ヘッダーなど残存箇所のみ） |

### 全画面共通の背景システム（2026-05-09 〜）

**「ラベンダーミスト」世界観**をすべての画面で共有する。

```css
/* body に background-attachment: fixed で全画面に統一適用 */
body {
  background-color: #EAE5F2;
  background-image:
    radial-gradient(ellipse 70% 45% at 50% 6%,  rgba(255,255,255,0.92) 0%, transparent 60%), /* 上中央の白いグロー */
    radial-gradient(ellipse 65% 42% at 50% 0%,  rgba(220,210,255,0.55) 0%, transparent 65%), /* 頂点の薄紫 */
    radial-gradient(ellipse 80% 50% at 12% 95%, rgba(180,165,255,0.18) 0%, transparent 55%), /* 左下の薄い余韻 */
    radial-gradient(ellipse 60% 45% at 90% 8%,  rgba(249,168,201,0.10) 0%, transparent 50%); /* 右上の微ローズ */
  background-attachment: fixed;
}
```

各 `.screen` は `background-color: transparent; position: relative; z-index: 1` で body のミストと装飾レイヤーの前面に表示する。

### 全画面共通の装飾レイヤー `#ambient-decor`（2026-05-10 〜）

`position: fixed; inset: 0; pointer-events: none; z-index: 0` の単一固定レイヤー。全画面で常時表示。画面遷移しても粒子位置が変わらないため、連続した「アプリの空気感」として機能する。

| 要素 | 内容 |
|---|---|
| `::before` | 上中央のソフトグロー（`radial-gradient` + 7.2s 周期で `glow-breathe` 呼吸アニメ） |
| `.amb-star` × 14個 | 丸ぼかし光粒（`star-twinkle` で opacity/scale をフェード） |
| `.amb-sparkle` × 8個 | 十字キラ（`::before`縦光 + `::after`横光、`sparkle-twinkle` で回転しながらフェード） |
| `.amb-mist--a/b` × 2個 | 大きな blur 楕円（`mist-drift-a/b` で 18〜22s かけてゆっくり漂う） |

粒子は `spawnAmbientDecor()`（app.js）が DOMContentLoaded 直後に1回生成。配置は縁寄り優先（コンテンツ中央帯を避ける）。

`prefers-reduced-motion: reduce` 適用時: `.amb-star / .amb-sparkle / .amb-mist` を `display: none`、`::before` のアニメを停止。

~~ウェルカム専用の `#screen-welcome::before` と `.welcome-glow`~~ → ambient-decor に統合（2026-05-10 〜）。

**分離設計の原則**:
- 全画面共通トーン（背景） ✕ 画面固有の色（ムードカード内部、入力CTAグラデ）は独立。
- モードカード（ネイビー/モーブ/ゴールデン）は内部配色を維持したまま紫ミスト上に浮かび上がる。

### ムードカード配色（色彩心理学に基づく）

| モード | 色 | 根拠 |
|---|---|---|
| しんどい感じ | `#060A12 → #1E5EAE`（ネイビーブルー） | Jonauskaite et al. 2020 — 憂鬱感・不快感との色-感情対応 |
| うまく言えない | モーブパープル（`#EDE9F2` 系） | 非言語・曖昧・霧感のニュアンス |
| いい感じ | `#FFFBEB → #F59E0B`（ゴールデンイエロー） | 128年分メタ分析 2024 — 喜び・活力との対応 |

### カードデザイン（Apple ライク・やわらかシール感）

**2026-05-09 リデザイン** — ウェルカム画面の Apple ライク路線に全画面を統一。  
**2026-05-10 リデザイン** — SVGイラストをラベンダーミスト世界観に完全統一。

**SVGイラスト（2026-05-10 〜）**: 全40枚+、ラベンダーミストパレット統一SVG（46×46px）

#### イラスト統一パレット

| 用途 | カラー | 使い所 |
|---|---|---|
| 主線（lavender ink） | `#9B8FEA` | すべての輪郭線・主要ストローク |
| 副線（pearl outline） | `#C4B5FD` | 補助線・繊細な装飾 |
| 主面（mist fill） | `#EDE8FA` | 形のメインの塗り |
| 深面（deep mist） | `#DDD6FE` | 影・奥行きの面 |
| クリーム煌めき | `#FDE68A` | スター・スパークル（全モード） |
| 淡ピンク煌めき | `#F9A8C9` | 補助スパーク（**いい感じモード専用**） |
| 薄青ミスト | `rgba(168,196,229,0.7)` | しんどい側のひんやり感（雨・霧） |
| くすみグレー | `#B0A8C0` | うまく言えない側のニュートラル輪郭 |

**禁止色**: `#EF4444 / #FCA5A5 / #6EE7B7 / #34D399 / #059669 / #F59E0B / #FB923C / #FBBF24 / #4338CA / #6366F1 / #818CF8 / #DB2777 / #F472B6 / #60A5FA / #38BDF8`（旧来の原色・鮮彩色）

**顔キャラクター**: 全廃。代わりに抽象的なシルエット（月・雲・葉・波線・盾など）とグロー演出で感情を表現。

**モード別温度差**:
- **しんどい**: ラベンダー＋薄青ミストで「ひんやり・重さ」を表現。クリームスパークルは控えめ。
- **いい感じ**: ラベンダー＋クリーム（`#FDE68A`）＋淡ピンク（`#F9A8C9`）のスパークルで「温かみ・軽さ」を表現。
- **うまく言えない**: くすみグレー（`#B0A8C0`）の輪郭＋靄の塊で「言葉にならなさ」を表現。

**共通装飾**: 各イラストに2〜3個の小さな星またはドット（`#FDE68A` または `rgba(255,255,255,0.6)`）を散らし、ウェルカム画面の星屑と統一感を持たせる。

**スタイル基本仕様**:
- 角丸: **18px**（旧 22px。Apple の cornerRadius に寄せる）
- ボーダー: **0.5px hairline** `rgba(30,27,46,0.06)`（旧 3px 白 + 1.5px outline の二重線）
- 影: 接地影 + アンビエント + inset 上端ハイライト の3層
- 微回転: `nth-child` 6パターンで **−0.8° 〜 +0.9°**（旧 ±1.8°。約半分に弱め、温かみを残しつつ整然さを確保）
- ラベル: `font-size: 12.5px / font-weight: 700 / color: var(--ink-1)`

**カード上端アクセントバー**（`::before`）:
- 各カードの上端に `height: 2px` の極細カラーバーを配置。症状の色連想はこの1本に集約。
- hover 時: 左右に広がる（`left: 8%; right: 8%`）
- 通常時: `left: 14%; right: 14%`（控えめに）

**選択状態（2026-05-09 〜）**:
- 旧: ブランドグラデで全面塗りつぶし → 廃止
- 新: **白基調 + `--brand` 細枠 + 3px アウトリング + 右上チェックバッジ**（`::after` でCSS描画）
  - チェックバッジ: 18×18px 円形、`--brand` 背景 + 白チェックマーク SVG、ポップインアニメーション
  - ラベル色: `var(--brand)` に変化
  - アイコン: 色彩を保ったまま（旧の brightness(0) invert を廃止）
- 選択時の微回転: 約半分（-0.4° 〜 +0.45°）+ `scale(1.02)`

**カード別カラートークン（2026-05-10 〜）**:

旧来の症状別40+ルール（`[data-value="XXX"]` セレクタ）を廃止し、**モード別3ルール**に集約。

```css
/* 基底値（フォールバック） */
.sel-card { --card-tint: #F4F0FA; --card-accent: #B39DFA; }

/* モード別トーン */
#set-unwell .sel-card { --card-tint: #EFEAF6; --card-accent: #A8AEDC; } /* 寒色ラベンダー */
#set-good   .sel-card { --card-tint: #F4EEF7; --card-accent: #C7B7EE; } /* 暖色ラベンダー */
#set-unsure .sel-card { --card-tint: #F1EFF4; --card-accent: #B0A8C0; } /* くすみグレー   */
```

上端2px バー（`::before`）の色連想は引き続きこの `--card-accent` から生成される。症状ごとの個別色付けは廃止し、世界観の統一を優先する。

### ムードカードのアンビエントドリフト（ambient drift）

3D チルト（JS + DeviceOrientation）は **完全撤去**。  
代替として CSS `@keyframes ambient-drift` による18秒ループのライト拡散アニメーション。

```css
.mood-card::before {
  content: '';
  position: absolute;
  /* 光源が 5ウェイポイントをゆっくり移動（振幅を半分に抑制: 2026-05-09 〜） */
  animation: ambient-drift 18s ease-in-out infinite;
}
@keyframes ambient-drift {
  0%, 100% { transform: translate(0,    0   ); }
  20%       { transform: translate(90px,  15px); }
  40%       { transform: translate(110px, 90px); }
  55%       { transform: translate(70px,  120px); }
  75%       { transform: translate(10px,  100px); }
}
```

ホバー時: `translateY(-2px)` のリフトのみ（旧 -3px → -2px に控えめ化）。

### フィードバックブロックの遅延表示

```css
#feedback-block        { opacity: 0; transition: opacity 540ms ease; }
#feedback-block.appeared { opacity: 1; }
```

翻訳完了から 1800ms 後に JS が `.appeared` クラスを付与。  
理由: 翻訳文を先に読む時間を確保し、感情への共鳴余地を作る（チェックリスト化を防ぐ）。

### コンポーネントリファイン一覧（2026-05-09 〜）

| コンポーネント | 変更前 | 変更後 |
|---|---|---|
| `.back-btn` | 36px 白カード / 1.5px ボーダー | 34px / `backdrop-filter: blur(12px)` のガラス調 |
| `.section-eyebrow` | 12px グラデドット + 15px / 800 | グラデドット廃止、13px / 700 / letter-spacing 0.06em + hairline 区切り線 |
| `.page-title` | 28px / 800 / tracking -0.03em | 30px / 600 / tracking -0.02em（Apple hero 見出しの軽さ） |
| `.mood-toggle` | `background: var(--border)` | `rgba(30,27,46,0.06)` の軽量化、active 影を Apple 影スタック |
| `.memo-field` | 1.5px ボーダー | 0.5px hairline + 2層ソフト影 |
| `.parent-panel` | dashed 1.5px ボーダー | solid 0.5px hairline + ブランド tint 背景 (`rgba(124,111,224,0.18)`) |
| `.feedback-btn` | 白地 / 1.5px ボーダー | グレー薄地 `rgba(30,27,46,0.025)` / hairline、選択時 brand tint + 枠 |
| `.share-action-btn` | 白地 / 1.5px ボーダー | 白地 / 0.5px hairline / 2層ソフト影 |
| `.ghost-btn` | 1.5px ボーダー | 0.5px hairline、hover 時 glass 背景 |
| `.mood-card-brand` | letter-spacing 0.07em | 0.18em（Apple caption 風） |

### コンポーネントリファイン一覧（2026-05-10 〜）

| コンポーネント | 変更前 | 変更後 |
|---|---|---|
| `ICONS` 全40枚+ | 顔キャラクター＋原色（緑/橙/赤/青/黄） | 顔キャラ廃止。ラベンダーミスト単一パレット（`#9B8FEA` / `#EDE8FA` / `#DDD6FE`）＋クリーム/ピンクスパーク |
| カード色CSS | 症状別40+ルール（`[data-value="XXX"]`） | モード別3ルール（`#set-unwell` / `#set-good` / `#set-unsure`） |
| 全画面装飾レイヤー | ウェルカム専用 `.welcome-star` ×7 + 静的グロー | `#ambient-decor`（fixed/z:0）に統合。丸14・十字キラ8・ミスト2の3層構成 |
| カード初期表示 | 全カードを一覧表示 | 先頭3枚のみ表示 + 「もっと選ぶ（+N）」で展開（`#set-unwell` / `#set-good`） |
| クイック記録 | なし | 「選べないくらいしんどい…」ボタン（しんどいモード限定、カード選択不要で翻訳完了） |
| `translate()` 空タグ時 | `'はっきりとした原因はわからないが、今日は調子がよくない状態です。'` | `'今日はとても体調がつらく、症状を言葉にするのも難しいほどしんどい状態にある。'` |
| `spawnWelcomeStars()` | ウェルカム画面限定、`.welcome-wrap` 配下7個 | `spawnAmbientDecor()` にリネーム。`#ambient-decor` 配下に全粒子生成 |
| 翻訳中画面 | ゴーストキャラ（バウンス）+ 3色ドット | キャラ廃止。中央の光のオーブ + 拡散リング2層のミニマル構成。`#ambient-decor` のキラキラと連続 |
| しんどいトグル絵文字 | 😔（pensive face） | 🥹（face holding back tears）相当のSVG — 困り眉 + 潤んだ目（白ハイライト入り） + 波打つ口元 |
| `.feedback-block` / `.feedback-btn` | 純白カード + グレー hairline | 半透明白 + backdrop-blur + ラベンダー hairline（glass化） |
| `.share-action-btn` 4枚 | 純白カード + 絵文字（👨‍👩‍👧 / 🏥 / 🏫 / 🖼） | glass化 + 自作SVGアイコン（入力カードと同じラベンダーミスト世界観） |
| `.ghost-btn` | 透明背景 + グレー hairline | 半透明白 + backdrop-blur + ラベンダー hairline（glass化） |
| 翻訳結果のフィードバック | 3択タップ → 返信メッセージのみ。リカバリー導線なし | 「ちがう」「近い」選択時に `#feedback-refine` 展開（任意 textarea + 「言いなおしてみる」ボタン）。入力画面へ状態保持で戻れる |
| record schema | `{id, date, mode, tags, memo, sleepHours, sleepScore, signals, translation}` | 上記に加え `feedback?: 'wrong'\|'close'\|'exact'`、`refineNote?: string` を追記（任意） |

### アクセシビリティ

- 全ての操作ボタンに `aria-label` を付与
- モード切り替えトグルは `role="tablist"` / `role="tab"` / `aria-selected`
- フォーカス時の輪郭: `focus-visible` のみ（マウス操作では非表示）、`--brand` カラーの2pxアウトライン
- `prefers-reduced-motion: reduce` メディアクエリで全アニメーション無効化（ambient-drift / bounce / card-check-pop / glow-breathe / star-twinkle / sparkle-twinkle / mist-drift すべて）
  - `.amb-star / .amb-sparkle / .amb-mist` を `display: none`（2026-05-10 〜）
  - 微回転も完全解除（2026-05-09 〜 の `scale(1.02)` 固定値はそのまま流用）

---

## 設計ポリシー

### UI/UX 心理学原則（本アプリへの適用）

1. **認知負荷の最小化**: カードは「選ぶだけ」。タップ1回で意思表示が完結する。初期3枚表示で「全部見なくていい」を視覚的に保証。
2. **感情への共鳴余地**: フィードバックを1.8秒遅らせ、翻訳文を読む時間を先に与える。チェックリスト的処理を避ける。
3. **記録の圧力をゼロにする**: 「今日は記録しない」ボタンを最初の画面から提供。達成感より「聴いてきた」という傾聴フレームにストリークを変換。
4. **二項対立の緩和**: 「しんどい / いい感じ」の2択に収まらない日のための第3モード。カード0枚でもメモだけで翻訳できる。
5. **底の日の脱出口**: 選択肢を見ることすら辛い日に「選べないくらいしんどい…」1タップで記録を完了させる。「底であること自体を記録する」という行為を可能にする（2026-05-10 〜）。
6. **親子の分離**: 保護者向け客観値フィールドはフォーム末尾の折りたたみセクションに配置し、子どものメインフローに割り込まない。
7. **前回記憶による心理的安全**: 保護者パネルに前回入力値を参照表示することで「また一から入力」の摩擦を軽減。
8. **病名暗示の制御**: 学校向け文面には配慮事項のみ、医師向けも「典型像と類似」止まり。過度な医療化を避ける。

### 言語・文体ポリシー

- 子どもへの語りかけは平仮名多用・体言止め禁止・命令形禁止
- 共有文面の医学的表現は医師向けのみ。親・学校向けは「眠りの様子: 短め」等のソフト表現
- ストリーク文言: 義務感（「達成」「継続」）ではなく傾聴感（「声を聞いてきた」）

### データアーキテクチャポリシー

- **子ども側の語彙を変えない**: シグナル・所見・サマリは医師/学校向け文面でのみ表面化
- **客観値はあくまで補助**: 主観カードを否定しない。「客観X時間 / 主観: ◯◯」と並列表示
- **病名は暗示まで**: 学校向けは「教室での配慮」、医師向けも「典型像と類似」
- **保護者の手入力で受ける**: HealthKit / Health Connect の直接連携はWeb制約上見送り
- **うまく言えないモードのシグナル検出はスキップ**: 汎用カードへのシグナル帰属は誤検出リスクが高い

---

## ダークモード対策

3段構え（`<meta name="color-scheme">` + `:root` + `@media (prefers-color-scheme: dark)`）で完全上書き。

ダークモードでも **ライトモードのラベンダーミスト** をそのまま維持する（暗くしない）。
`@media (prefers-color-scheme: dark)` ブロック内で `html, body` に `background-color: #EAE5F2 !important` と同じミスト background-image を指定。

---

## 今後の拡張候補

### 優先度: 高
- [ ] Claude APIへの切り替え（モック → リアルAI）
- [ ] 「画像で保存」機能（html2canvas or Canvas API）
- [ ] 記録の履歴画面（過去の翻訳カードを一覧表示・kmt_records を可視化）

### 優先度: 中
- [ ] 音声入力対応（Web Speech API）
- [ ] PWA化（manifest.json、Service Worker）
- [ ] HealthKit / Health Connect 連携の検討
- [ ] フィードバックデータの収集・分析（翻訳の粒度実験）

### 優先度: 低 / 検討中
- [ ] 親・医師用の閲覧専用ビュー
- [ ] 通知リマインダー（毎日のプッシュ通知）
- [ ] 体調トレンドのグラフ表示
- [ ] **`circadian` シグナルの判定強化（DSPS的パターンの取り込み）**
      参考: 志村哲祥『寝た子は起こすな「早起き神話」の深刻な現実』NHK出版, 2026  
      思春期の睡眠相後退（DSPS）的パターンをより的確に検出。  
      現状は `朝起きられない ∧ (寝つけなかった ∨ 夜中に起きた ∨ 眠れなかった)` だが、  
      「夜になると元気・冴える」を捉えるカード/メモ解析を加え、  
      前傾（早朝覚醒）と後傾（夜型化）を区別できるとより精緻になる。

---

## 参考文献・着想元

設計判断の根拠として参照した一次資料を以下に記録する。実装に直接根拠を持たせるためのものであり、医療的判断の代替ではない。

- **志村哲祥『寝た子は起こすな「早起き神話」の深刻な現実』NHK出版, 2026年4月10日, 200ページ**
  - 著者は東京医科大学睡眠学講座客員教授、スタンフォード大学で睡眠研究を実施した経歴を持つ。
  - 第5章「病気としての『朝起きられない』」、第4章「概日リズムの睡眠への影響」が、本アプリの主軸ターゲット（OD・概日リズム障害）と直接対応。
  - 学校向け文面の【背景にある発達特性】節（思春期の体内時計後ろずれの説明）の根拠としている。
  - シンギュラボメンバーからの情報提供。

- **株式会社こどもみらい (https://cfltd.co.jp)**
  - STRESCOPE / eSLEEP / カラダスコープ / 学校保健の事業領域に着想を得て、  
    症状の組み合わせシグナル・睡眠カテゴリの独立化・時系列傾向サマリ・提出先別トーン切替を設計。
  - シンギュラボメンバーからの情報提供。

- **色彩心理学資料**
  - Jonauskaite, D. et al. (2020). Universal patterns in color-emotion associations. *Psychological Science*.（憂鬱感とネイビーブルーの対応）
  - Meta-analysis on color-emotion (2024).（喜び・活力とゴールデンイエローの対応、128年分のデータ）

---

## プレビュー起動方法

```bash
# プレビューサーバー（Claude Codeのpreview_startで自動起動）
# URL: http://localhost:3002
```

または `index.html` をブラウザで直接開く（ビルド不要）。

---

## デプロイ

**本番URL**: https://ops324.github.io/kimochi-honyaku/  
**リポジトリ**: https://github.com/ops324/kimochi-honyaku  
**ホスティング**: GitHub Pages（main ブランチのルートを直接配信）

### 更新手順

```bash
git add -p          # 変更を確認しながらステージ
git commit -m "..."
git push            # プッシュ後 1〜2 分で本番に反映
```

ビルドステップなし。CI/CD 設定不要。
